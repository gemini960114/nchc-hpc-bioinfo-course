# 國網中心 HPC × Antigravity IDE × AI Agent 生醫運算實戰課程講義

- **課程日期**：2026-09-08
- **講義代號**：`course_20260908.md`
- **適用環境**：Antigravity IDE (VS Code 相容環境) / Windows 10 & 11 / 國網中心台灣杉三號 (Taiwania 3, T3-C4) 生醫節點 / 晶創25 (TAIDE/HPC)

---

## 📑 課程目錄

1. [單元一：Antigravity IDE 現代化開發介面導覽](#unit-1)
   - [1.1 左側主側邊欄（Explorer, Extensions, Remote Explorer）](#unit-1-1)
   - [1.2 中間編輯區（文件與 Notebook）](#unit-1-2)
   - [1.3 右側次側邊欄（AI Agent Panel）](#unit-1-3)
2. [單元二：Remote SSH 遠端連線設定（.ssh/config 與 ssh-proxy）](#unit-2)
   - [2.1 新增與設定 ~/.ssh/config](#unit-2-1)
   - [2.2 下載 ssh-proxy 工具 (v0.3.2)](#unit-2-2)
   - [2.3 啟動 Proxy 連線實作步驟](#unit-2-3)
3. [單元三：必備 Extensions 延伸模組安裝](#unit-3)
   - [3.1 AI 智能輔助類（ChatGPT, Claude, Roo Code / zoocode）](#unit-3-1)
   - [3.2 Python 與 Jupyter 運算類](#unit-3-2)
4. [單元四：Jupyter Notebook 實戰與 ipykernel 核心管理](#unit-4)
   - [4.1 核心概念：為什麼需要 ipykernel？](#unit-4-1)
   - [4.2 建立 Conda / venv 環境並註冊 Kernel](#unit-4-2)
   - [4.3 在 Antigravity IDE 中切換與測試 Kernel](#unit-4-3)
5. [單元五：SLURM 排程系統語法與批次作業教學](#unit-5)
   - [5.1 HPC 基本運作機制：Login Node vs. Compute Node](#unit-5-1)
   - [5.2 SLURM 腳本核心語法與 #SBATCH 參數](#unit-5-2)
   - [5.3 平台規格對比：台灣杉三號生醫節點 vs. 晶創25](#unit-5-3)
   - [5.4 架構實務：邏輯腳本與派送腳本分離](#unit-5-4)
   - [5.5 常用 SLURM 監控與管理指令](#unit-5-5)
6. [單元六：AI Agent 輔助生醫 HPC 開發實戰](#unit-6)
   - [6.1 從 Chatbot 到自主 Agent（Agentic Workflow）](#unit-6-1)
   - [6.2 Antigravity Agent 與 Roo Code (zoocode) 實務應用](#unit-6-2)
   - [6.3 生醫 HPC 實用 Prompt 模板](#unit-6-3)
7. [單元七：課程補充講義索引與延伸學習](#unit-7)

---

## 單元一：Antigravity IDE 現代化開發介面導覽 {#unit-1}
<span id="單元一antigravity-ide-現代化開發介面導覽"></span>

Antigravity IDE 是一套基於 VS Code 相容架構並深度整合次世代 AI Agent 與雲端遠端運算能力的現代化開發工具。其核心介面劃分為三大關鍵工作區：

```
+-----------------------------------------------------------------------------------+
|  功能表列 (File, Edit, Selection, View, Go, Run, Terminal, Help)                  |
+---+----------------------+---------------------------------+----------------------+
| 側| [主側邊欄]           | [中間編輯區]                     | [次側邊欄 - AI Agent]|
| 邊|                      |                                 |                      |
| 圖| - 檔案總管 (Explorer)| - 程式原始碼 (Python, Bash...)  | - 自然語言對話視窗   |
| 示| - 搜尋 (Search)      | - Markdown 即時預覽             | - 專案程式碼檢索     |
| 列| - 原始碼控制 (Git)   | - 互動式 Jupyter Notebook       | - 檔案自動讀寫與重構 |
|   | - 延伸模組(Extension)| - 分割檢視 (Split Editor)       | - 終端指令排程與執行 |
|   | - 遠端總管 (Remote)  |                                 |                      |
+---+----------------------+---------------------------------+----------------------+
|  底層狀態列 (Status Bar: 遠端連線狀態、Git 分支、Python 直譯器/Kernel)             |
+-----------------------------------------------------------------------------------+
```

### 1.1 左側主側邊欄（Primary Side Bar） {#unit-1-1}
左側側邊圖示列（Activity Bar）包含常用的檔案與專案管理功能：
1. **檔案總管（Explorer, `Ctrl + Shift + E`）**：
   - 檢視與管理本機或遠端 HPC 主機上的工作目錄（Workspace）、檔案與子目錄。
   - 支援拖曳上傳檔案、建立新檔、批次重新命名與 Git 狀態標記（修改為藍色、新增為綠色）。
2. **延伸模組市集（Extensions, `Ctrl + Shift + X`）**：
   - 搜尋、安裝、更新與停用各種擴充套件（如 Python、Jupyter、AI 工具等）。
3. **遠端總管（Remote Explorer）**：
   - 集中列出與管理已設定的 SSH 遠端主機（如 `t3-c4`、`t3-c4-proxy`）。
   - 點選主機旁邊的「連線（在新視窗開啟）」圖示即可開啟遠端工作階段。

### 1.2 中間編輯區（Editor Area） {#unit-1-2}
- **多分頁與網格分割**：支援拖曳分頁將視窗垂直或水平分割（`Ctrl + \`），可同時一邊對照講義 Markdown，一邊編寫 Shell 或 Python 腳本。
- **Jupyter Notebook 原生支援**：開啟 `.ipynb` 檔案時，中間編輯區會自動載入互動式 Cell 單元，支援直接編譯 Markdown 說明與執行 Python / Bash 代碼。
- **內建終端機（Terminal, `Ctrl + ` `）**：按下快速鍵可在編輯區下方呼叫出 Shell 終端機（本機為 PowerShell，連上 HPC 後自動轉為 Bash 互動命令列）。

### 1.3 右側次側邊欄（AI Agent Panel） {#unit-1-3}
Antigravity IDE 將 AI Agent 原生設計於右側次側邊欄（Secondary Side Bar），提供不打擾主要代碼編輯的強大輔助：
- **開啟方式**：
  - 頂端選單：點擊 `View` → `Appearance` → `Secondary Side Bar`
  - 快捷鍵：`Ctrl + Alt + B`（或視窗右上角的側邊欄開關按鈕）
- **核心能力**：
  - **Context-Aware**：自動辨識你目前正在檢視與游標所在的檔案行號。
  - **Tool Use / Agentic Actions**：Agent 擁有自主搜尋工作區目錄、讀取檔案、產生多處差異修訂（Diff）、甚至在取得同意後執行 Shell 指令的高階功能。

---

## 單元二：Remote SSH 遠端連線設定（.ssh/config 與 ssh-proxy） {#unit-2}
<span id="單元二remote-ssh-遠端連線設定sshconfig-與-ssh-proxy"></span>

國網中心台灣杉三號生醫專用節點（`t3-c4.nchc.org.tw`）為了資訊安全，採用了**多因素驗證 (MFA / 2FA OTP)** 機制，且登入節點不開放 SSH Key 金鑰直接登入，必須使用密碼 + OTP 互動式驗證。

若使用一般 VS Code / Antigravity IDE 內建的 Remote SSH，每次重新連線或開啟新視窗都會反覆詢問 OTP。因此我們提供兩種連線模式：
1. **直連模式 (`t3-c4`)**：適合偶爾連線、單一終端操作。
2. **Proxy 代理模式 (`t3-c4-proxy`)**：搭配本機 `ssh-proxy` 工具，**OTP 只需要在啟動 proxy 時輸入一次**，後續 IDE 連線與背景重連完全無感，強烈推薦上課實作使用。

---

### 2.1 新增與設定 `~/.ssh/config` {#unit-2-1}

在 Windows 本機環境下，開啟個人目錄下的 `.ssh\config`：
- 完整路徑：`C:\Users\<你的 Windows 帳號>\.ssh\config`
- 若目錄或檔案不存在，請手動新增資料夾 `.ssh` 與純文字檔 `config`（無副檔名）。

將以下內容完整貼入 `config` 中（**請務必將 `<你的帳號>` 改為你在國網申請的主機帳號**）：

```ssh-config
# ==========================================
# 1. 直連模式：每次連線皆進入 OTP / MFA 流程
# ==========================================
Host t3-c4
  HostName t3-c4.nchc.org.tw
  User <你的帳號>

  # 直接進入 OTP / MFA 驗證流程（關閉金鑰，強制互動式密碼與 OTP）
  PubkeyAuthentication no
  KbdInteractiveAuthentication yes
  PreferredAuthentications keyboard-interactive,password

  # 維持及偵測連線狀態，避免連線閒置斷線
  ServerAliveInterval 30
  ServerAliveCountMax 3

  # 如果目前網路環境有封包品質問題可保留此設定
  IPQoS none

# ==========================================
# 2. Proxy 模式：先啟動本機 ssh-proxy，OTP 僅需輸入一次
# ==========================================
Host t3-c4-proxy
  HostName 127.0.0.1
  Port 2222
  User <你的帳號>

  # 本機 proxy 每次啟動會產生臨時 host key，忽略憑證警告
  StrictHostKeyChecking no
  UserKnownHostsFile /dev/null
  LogLevel ERROR

  # 偵測本機 proxy 是否已停止
  ServerAliveInterval 30
  ServerAliveCountMax 3
```

> [!WARNING]
> SSH Config 中的 `User <你的帳號>` **不會自動展開 Shell 環境變數**，請直接填寫文字（如 `User c00cjz00`），切勿填寫 `$USER` 或 `%USERNAME%`。

---

### 2.2 下載 `ssh-proxy` 工具 (v0.3.2) {#unit-2-2}

`ssh-proxy` 是由國網輔導團隊開發的連線轉發輔助工具，能大幅提升 IDE 在 MFA 環境下的穩定度。

- **官方下載頁面**：[https://github.com/gemini960114/ssh-proxy/releases/tag/v0.3.2](https://github.com/gemini960114/ssh-proxy/releases/tag/v0.3.2)
- **下載檔案**：`ssh-proxy-windows-x64.exe`
- **存放建議**：建議下載後存放在好找的資料夾，例如 `C:\tools\ssh-proxy\` 或個人專案目錄下。

---

### 2.3 啟動 Proxy 連線實作步驟 {#unit-2-3}

#### 步驟 1：在 Windows PowerShell 啟動 Proxy
開啟 Windows PowerShell，切換到 `ssh-proxy-windows-x64.exe` 所在的目錄，執行：

```powershell
.\ssh-proxy-windows-x64.exe t3-c4
```

#### 步驟 2：輸入國網主機密碼與 OTP 二次驗證
終端機將出現 2FA 驗證方式選單：
```text
Please select the 2FA login method.
1. Mobile APP OTP
2. Mobile APP PUSH
3. Email OTP
```
- 輸入對應數字（例如 `1`），接著輸入手機 App 產生的 6 位動態 OTP 驗證碼。
- 驗證成功後，PowerShell 會顯示 Proxy 已於 `127.0.0.1:2222` 監聽。
- **請保持此 PowerShell 視窗開啟，不要關閉！**

#### 步驟 3：在 Antigravity IDE 中連線
1. 點擊 Antigravity IDE 左側的 **Remote Explorer** 圖示。
2. 在 SSH 清單中，找到 **`t3-c4-proxy`**。
3. 點選其右側的 **Connect to Host in New Window（在新視窗開啟連線）**。
4. IDE 會透過本地 `127.0.0.1:2222` 轉發至遠端，**不會再要求輸入密碼或 OTP**，數秒內即可成功連入！
5. 連線成功後，點擊左側 Explorer 的 `Open Folder`，輸入 `/work/<你的帳號>` 即可直接瀏覽與編輯遠端伺服器上的大容量工作目錄。

---

## 單元三：必備 Extensions 延伸模組安裝 {#unit-3}
<span id="單元三必備-extensions-延伸模組安裝"></span>

為了在 Antigravity IDE 中發揮完整的雲端生醫分析與 AI 輔助開發效能，請至左側活動列的 **Extensions**（快捷鍵 `Ctrl + Shift + X`）搜尋並安裝以下重點套件：

### 3.1 AI 智能開發助手類 {#unit-3-1}

| 延伸模組名稱 | 搜尋關鍵字 / ID | 說明與適用情境 |
|---|---|---|
| **ChatGPT** | `ChatGPT` / `chatgpt` | 提供通用的程式碼解說、語法翻譯與日常詢問介面。 |
| **Claude** | `Claude` / `anthropic` | 適合長文本閱讀、完整專案架構邏輯分析與文獻摘要。 |
| **Roo Code (zoocode)** | `Roo Code` / `Roo Cline` | **極力推薦**。具備自主 Agent 功能，能獲取權限直接檢視工作區檔案、執行終端機命令、批次修改腳本並執行自動除錯循環。 |

### 3.2 Python 與 Jupyter 核心運算類 {#unit-3-2}

| 延伸模組名稱 | 識別碼 ID | 必要性 | 說明 |
|---|---|---|---|
| **Jupyter** | `ms-toolsai.jupyter` | **必裝** | 提供 `.ipynb` 筆記本的互動式執行介面與資料可視化輸出。 |
| **Python** | `ms-python.python` | **必裝** | 提供 Python 代碼高亮、自動補完（IntelliSense）與虛擬環境識別。 |
| **Jupyter Keymap** | `ms-toolsai.jupyter-keymap` | 推薦 | 還原傳統 Jupyter 鍵盤習慣（`Esc` 進入指令模式、`B` 插入下方 Cell、`DD` 刪除 Cell、`Shift + Enter` 執行）。 |
| **Jupyter Cell Tags** | `ms-toolsai.vscode-jupyter-cell-tags` | 推薦 | 支援在 Notebook Cell 上打標籤（Tags），方便投影片製作或管線分類。 |

> [!TIP]
> 當透過 Remote SSH 連線至 `t3-c4-proxy` 時，延伸模組分為「本機端（UI 擴充）」與「遠端主機端（Remote 擴充）」。像 `Python` 與 `Jupyter` 建議點擊 **Install in SSH: t3-c4-proxy**，直接安裝於遠端環境，以獲得最佳效能。

---

## 單元四：Jupyter Notebook 實戰與 ipykernel 核心管理 {#unit-4}
<span id="單元四jupyter-notebook-實戰與-ipykernel-核心管理"></span>

### 4.1 核心概念：為什麼需要 ipykernel？ {#unit-4-1}

在生醫與資料科學開發中，常見的痛點是：「終端機裡的 conda/venv 套件明明裝好了，為什麼在 Jupyter Notebook 裡面 `import` 卻報錯 `ModuleNotFoundError`？」

其核心原因在於：**IDE 的前端介面** 與 **後端實際執行代碼的 Python 環境** 是分離的：
- **Jupyter 擴充套件**：只負責顯示 Notebook 的 Cell 介面。
- **ipykernel**：是溝通 Notebook 與 Python 直譯器的核心橋樑。
- 每一個 Conda 環境或虛擬環境，都必須安裝 `ipykernel` 並將自己註冊為系統可見的 **Kernel Spec**，Antigravity IDE 才能在選單中精確掛載它。

---

### 4.2 建立 Conda 環境並註冊 Kernel（以 QIIME2 生醫環境為例） {#unit-4-2}

在遠端連線終端機中，切換至大容量暫存目錄 `/work/$USER`：

#### 步驟 1：啟用 Conda 環境
```bash
# 載入模組或啟動個人安裝的 Miniconda
source /work/$USER/Miniconda/bin/activate

# 切換至目標分析環境（例如 QIIME2 生醫環境）
conda activate rachis-qiime2-2026.7
```

#### 步驟 2：安裝 ipykernel
```bash
# 在該環境中安裝 ipykernel
conda install -y ipykernel
```

#### 步驟 3：註冊成全域可辨識的 Jupyter Kernel
```bash
python -m ipykernel install --user \
  --name rachis-qiime2-2026.7 \
  --display-name "QIIME2 (2026.7)"
```
- `--name`：系統識別的唯一名稱（英數與底線）。
- `--display-name`：在 IDE 介面右上角顯示的使用者友善名稱（如 `"QIIME2 (2026.7)"`）。

#### 步驟 4：檢查註冊清單
```bash
jupyter kernelspec list
```
輸出中若看到 `rachis-qiime2-2026.7` 即代表註冊成功！

---

### 4.3 在 Antigravity IDE 中切換與測試 Kernel {#unit-4-3}

1. 在工作區中點擊右鍵新增檔案，命名為 `test_kernel.ipynb`。
2. 開啟檔案後，觀察右上角的 **Kernel 選取器**（若顯示 `Select Kernel`，請點擊它）。
3. 選擇 **Python Environments...** 或 **Jupyter Kernel...**。
4. 從清單中點選剛剛註冊的 **QIIME2 (2026.7)**。
5. 在第一個 Cell 中輸入以下程式碼並按下 `Shift + Enter` 執行驗證：

```python
import sys
print("目前使用的 Python 直譯器路徑：", sys.executable)

# 測試環境是否能正確讀取生醫套件
import qiime2
print("QIIME2 版本：", qiime2.__version__)
```

6. **在 Notebook 中執行 Shell 指令小技巧**：
   - 單行指令加驚嘆號：`!which qiime`
   - 整格以 Bash 執行（支援多行）：
     ```bash
     %%bash
     echo "目前工作目錄: $(pwd)"
     echo "目前使用者: $USER"
     ```

---

## 單元五：SLURM 排程系統語法與批次作業教學 {#unit-5}
<span id="單元五slurm-排程系統語法與批次作業教學"></span>

### 5.1 HPC 基本運作機制：Login Node vs. Compute Node {#unit-5-1}

國網中心高效能叢集（如台灣杉三號、晶創25）由數百台伺服器組成：

```
+-------------------------------------------------------------+
| [使用者端電腦] (Windows / macOS / Antigravity IDE)          |
+-------------------------------------------------------------+
                              | SSH (Port 22 / Proxy 2222)
                              v
+-------------------------------------------------------------+
| [登入節點 (Login Node)] (如 t3-c4 / lgn304)                  |
| - 用途：程式碼編寫、檔案傳輸、版本控制、送出作業             |
| ⚠️ 禁止事項：不可直接在此執行大量 CPU/GPU 密集計算            |
+-------------------------------------------------------------+
                              | SLURM 排程器派送 (sbatch)
                              v
+-------------------------------------------------------------+
| [計算節點叢集 (Compute Nodes)] (如 cpn3859, cpn3852...)      |
| - CPU 節點 (56 核心, 384G RAM)                               |
| - GPU 節點 (NVIDIA V100 / A100 / H100)                      |
| - 大記憶體節點 (2TB ~ 6TB RAM)                              |
+-------------------------------------------------------------+
```

> [!CAUTION]
> **嚴禁在登入節點直接跑重負載運算！**
> 登入節點是數十位甚至上百位研究人員共享的入口。若在登入節點直接執行大數據分析、模型訓練或管線，將導致記憶體耗盡並被系統守護程序直接強制砍單（Kill），甚至影響他人連線。所有重度計算必須透過 SLURM 送至 Compute Node。

---

### 5.2 SLURM 腳本核心語法與 `#SBATCH` 參數 {#unit-5-2}

SLURM 腳本本質上是開頭包含 `#SBATCH` 資源宣告的 Shell 腳本。

```bash
#!/usr/bin/bash
# ==============================================================
# SLURM 資源配置宣告 (由 SLURM 守護程序解析)
# ==============================================================
#SBATCH -A MST109178             # 計畫代號 (Account/Project ID)
#SBATCH -J qc_analysis           # 工作名稱 (Job Name)
#SBATCH -p ngs53G                # 佇列分區 (Partition)
#SBATCH -c 8                     # 要求 CPU 核心數 (CPUs per task)
#SBATCH --mem=53g                # 要求記憶體 (Memory)
#SBATCH -o logs/job_%j.out       # 標準輸出記錄檔 (%j 會代換為 Job ID)
#SBATCH -e logs/job_%j.err       # 標準錯誤記錄檔
#SBATCH --mail-user=user@example.com  # 通知信箱
#SBATCH --mail-type=BEGIN,END,FAIL    # 收到通知的時機

# ==============================================================
# 真正的運算任務內容 (在計算節點上執行)
# ==============================================================
set -euo pipefail

echo "=== 工作開始時間: $(date) ==="
echo "執行節點: $(hostname)"
echo "SLURM_JOB_ID: $SLURM_JOB_ID"

# 載入生醫專用環境或模組
module load biology
module load FastQC/0.11.9

# 執行分析指令
fastqc -t 8 -o ./qc_reports ./raw_data/*.fastq.gz

echo "=== 工作結束時間: $(date) ==="
```

#### 常用 `#SBATCH` 參數速查：
- `-A <Project_ID>`：計畫計費扣款代號。
- `-J <Name>`：工作名稱，顯示於 `squeue` 清單。
- `-p <Partition>`：指定的資源池佇列。
- `-c <CPUs>`：分配之 CPU 核心數。
- `--mem=<Size>`：分配之記憶體（如 `53g`）。
- `--gres=gpu:<N>`：要求 GPU 張數（如 `--gres=gpu:1`）。
- `-o` / `-e`：輸出與錯誤 Log 路徑，`%j` 可避免重複覆蓋紀錄。
- `--array=1-10`：陣列作業（Array Job），透過 `$SLURM_ARRAY_TASK_ID` 同時平行處理多筆樣本。

---

### 5.3 平台規格對比：台灣杉三號生醫節點 vs. 晶創25 {#unit-5-3}

不同叢集在排程參數限制、網路與節點特性上有顯著差異（晶創25 詳細規格可參閱附檔 [晶創25使用者服務介紹-V1.0.pdf](晶創25使用者服務介紹-V1.0.pdf) 或 [GitHub 檢視 PDF ↗](https://github.com/gemini960114/nchc-hpc-bioinfo-course/blob/main/%E6%99%B6%E5%89%B525%E4%BD%BF%E7%94%A8%E8%80%85%E6%9C%8D%E5%8B%99%E4%BB%8B%E7%B4%B9-V1.0.pdf)）：

| 比較項目 | 台灣杉三號（T3-C4 生醫專用） | 晶創25（TAIDE / HPC 創新平台） |
|---|---|---|
| **主要定位** | 生醫資料處理、NGS 序列分析、結構生物學 | 生成式 AI、大型語言模型訓練、通用 HPC 雲端 |
| **登入認證** | 帳號密碼 + **OTP 雙因素驗證 (MFA)** | SSH Key 金鑰對 (`.pem` / `.pub`) 或 IAM 權限 |
| **Partition 規則** | **嚴格規格綁定**：`-c` 與 `--mem` 必須精準符合分區定義（如 `ngs53G` 必為 8 核 / 53GB） | 彈性指定資源上限，依需求配置核心與 GPU 配額 |
| **常見佇列** | `ngsTest`, `ngs7G`, `ngs53G`, `ngs186G`, `ngs1gpu`, `ngs4gpu` | `normal`, `gpu`, `interactive` |
| **計算節點連外網** | ⚠️ **預設無法直連外網**（需設定 Proxy：`export http_proxy=http://lgn304-v304:53128`） | 大多具備 NAT 網關或可連通公網儲存庫 |
| **搶佔機制** | **NRQ (Non-Reserved Queue)**：閒置資源釋出，生醫用戶送件時優先取回 | 標準 QoS 排程配額與優先權佇列管理 |

---

### 5.4 架構實務：邏輯腳本與派送腳本分離 {#unit-5-4}

在生醫大數據實務中，強烈推薦採用「兩層式腳本設計架構」：

```
專案目錄/
├── run_fastqc_multiqc.sh      # [邏輯腳本] 純 Bash 分析邏輯，可單機測試
└── slurm_fastqc_multiqc.sh    # [派送腳本] 僅含 #SBATCH 宣告與呼叫邏輯腳本
```

#### 為什麼要分開？
1. **除錯成本極低**：邏輯腳本可在登入節點上先拿小資料量測試語法，不必每次都送去 `sbatch` 排隊數十分鐘。
2. **遷移彈性高**：未來若更換 Partition（如從 8 核換成 16 核）或搬到晶創25，只需修改 `slurm_*.sh`，不需碰觸複雜的分析邏輯代碼。

**派送腳本範例 (`slurm_fastqc_multiqc.sh`)：**
```bash
#!/usr/bin/bash
#SBATCH -A MST109178
#SBATCH -J pipeline_fastqc
#SBATCH -p ngs53G
#SBATCH -c 8
#SBATCH --mem=53g
#SBATCH -o logs/pipeline_%j.out
#SBATCH -e logs/pipeline_%j.err

# 啟動邏輯腳本
bash run_fastqc_multiqc.sh
```

---

### 5.5 常用 SLURM 監控與管理指令 {#unit-5-5}

| 指令 | 說明 | 範例 |
|---|---|---|
| `sbatch <腳本.sh>` | 提交批次排程工作 | `sbatch slurm_job.sh` |
| `squeue -u $USER` | 查詢自己目前在 Queue 中的工作狀態 (PD: 排隊, R: 執行中) | `squeue -u $USER` |
| `scancel <Job_ID>` | 取消或強制終止工作 | `scancel 84684` |
| `scontrol show job <ID>` | 查詢特定工作之詳細即時狀態與節點分配 | `scontrol show job 84684` |
| `sacct -j <ID> --format=...` | 查詢工作資源消耗（記憶體最大使用量 `MaxRSS`、執行耗時） | `sacct -j 84684 -o JobID,State,MaxRSS,Elapsed` |
| `sinfo -s` | 檢視叢集各個 Partition 節點的閒置與負載狀態 | `sinfo -s \| grep ngs` |
| `qstat -ngs` | 查詢台灣杉三號生醫節點佇列狀態特製指令 | `qstat -ngs` |

---

## 單元六：AI Agent 輔助生醫 HPC 開發實戰 {#unit-6}
<span id="單元六ai-agent-輔助生醫-hpc-開發實戰"></span>

### 6.1 從 Chatbot 到自主 Agent（Agentic Workflow） {#unit-6-1}

在傳統的 AI 使用情境中，我們通常只是在瀏覽器詢問 ChatGPT：「請幫我寫一個 FastQC 腳本」，然後手動複製、貼上、除錯、再把錯誤訊息貼回問答視窗。

**現代 AI Agent（如 Antigravity IDE 內建 Agent 與 Roo Code）的核心演進：**
- **上下文感知 (Full Context)**：Agent 能直接存取你專案目錄的所有檔案、環境配置與日誌。
- **工具調用 (Tool Calling)**：Agent 具備呼叫工具的能力（讀檔、修改特定行號、執行終端指令、解析輸出結果）。
- **自我修復循環 (Self-Correction Loop)**：
  1. 送出 SLURM 腳本。
  2. 讀取 `logs/out_%j.err`。
  3. 發現報錯（如記憶體不足或模組未載入）。
  4. 自動調整 `#SBATCH --mem` 或修正指令，再次重新驗證。

---

### 6.2 Antigravity Agent 與 Roo Code (zoocode) 實務應用 {#unit-6-2}

#### 1. 工作區檢索與架構分析
直接在右側次側邊欄（Secondary Side Bar）向 Agent 提問：
> 「請檢視目前目錄下的所有 `.sh` 與 `.ipynb`，整理出一份分析流程圖，並檢查是否有寫死（hardcoded）的個人家目錄路徑。」

#### 2. SLURM 資源配置自動核對
> 「我要在台灣杉三號執行一項 QIIME2 diversity 分析，預估需要 8 個核心。請幫我撰寫符合生醫節點規範的 SLURM 派送腳本，確保記憶體與核心數完全對應 `ngs53G` partition，並加上日誌目錄防呆機制。」

#### 3. 執行報錯自動診斷與修復
當工作被 SLURM 終止時，將 Error Log 丟給 Agent：
> 「這是我剛剛執行的 SLURM 錯誤記錄：`slurmstepd: error: Detected 1 oom_kill event in StepId=84684.batch. Some of your processes may have been killed by the cgroup out-of-memory handler.` 請幫我分析原因並提供最佳的 Partition 調整建議。」

---

### 6.3 生醫 HPC 實用 Prompt 模板 {#unit-6-3}

你可以在右側次側邊欄直接複製以下提示詞與 AI Agent 協作：

#### 模板一：撰寫雙層管線腳本
```text
請為我規劃一個生醫資料分析流程：
1. 目的：針對 ./data 目錄下的雙端定序 (PE) fastq.gz 檔案執行 FastQC 與 MultiQC 匯總。
2. 架構要求：嚴格遵守「邏輯腳本 (run_qc.sh)」與「SLURM 派送腳本 (slurm_qc.sh)」分離架構。
3. 規範：
   - 邏輯腳本必須包含 set -euo pipefail
   - SLURM 派送腳本目標主機為台灣杉三號，請使用 MST109178 計畫代號與 ngs53G 分區。
```

#### 模板二：將 Notebook 轉換為 HPC 自動化排程
```text
請檢視目前的 demo.ipynb 檔案：
1. 將其中所有的資料清理與模型推論邏輯提取為獨立可執行的 Python 腳本 pipeline.py。
2. 撰寫一個 SLURM 腳本，透過 conda 啟用 rachis-qiime2-2026.7 環境並背景執行 pipeline.py。
3. 確保將輸出圖表儲存至 ./figures 目錄，並在完成後輸出 sacct 資源使用報告。
```

---

## 單元七：課程補充講義索引與延伸學習 {#unit-7}
<span id="單元七課程補充講義索引與延伸學習"></span>

本專案儲存庫中已備齊完整的環境配置、資料質檢與大數據管線手冊，學員可依需求點擊連結進一步深入研讀：

| 講義檔案 | 格式 | 涵蓋核心主題 | 在學習路徑中的定位 |
|---|---|---|---|
| [Jupyter_Notebook_Tutorial.md](00_environment_setup/Jupyter_Notebook_Tutorial.md) | Markdown | Antigravity IDE Jupyter 擴充套件安裝、主題視覺客製化、`uv` + venv + ipykernel 配置 | 通用 Jupyter 介面與快速環境入門 |
| [conda_miniconda_qiime2_install.md](00_environment_setup/conda_miniconda_qiime2_install.md) | Markdown | Miniconda 自行安裝、接受 Anaconda ToS 條款、QIIME2 conda 環境建置、ipykernel 註冊 | 生醫分析環境標準安裝 SOP |
| [Qiime2_VSCode_Jupyter_教學.md](00_environment_setup/Qiime2_VSCode_Jupyter_教學.md) | Markdown | 官方教學轉 Notebook 實作、Kernel 選取問題排查、`%%bash` 環境變數與 PATH 橋接 | 環境安裝 → Notebook 互動操作的實戰銜接 |
| [TWCC_Taiwania3_GP1生醫節點_使用手冊.md](00_environment_setup/TWCC_Taiwania3_GP1生醫節點_使用手冊.md) | Markdown | 帳號計畫申請、登入驗證、儲存空間 quota 與費率、模組系統、SLURM partition 配額表、連外網路 Proxy 設定 | **國網平台總覽手冊（最完整之架構與規範說明）** |
| [01_FastQC_MultiQC_教學.md](01_fastqc_multiqc/01_FastQC_MultiQC_教學.md) + [01_fastqc_multiqc_demo.ipynb](01_fastqc_multiqc/01_fastqc_multiqc_demo.ipynb) | MD + Notebook | 模擬 FASTQ 資料分析、FastQC 質檢與 MultiQC 整合報表判讀 | 生醫定序前置資料質檢核心實作 |
| [SLURM_腳本撰寫教學_QIIME2實作範例.md](02_qiime2_moving_pictures/SLURM_腳本撰寫教學_QIIME2實作範例.md) | Markdown | SLURM script 語法深解、邏輯與派送雙層解耦架構、Job 監控與 OOM 除錯技巧 | **HPC 排程實戰核心教材，後續模組沿用此架構** |
| [晶創25使用者服務介紹-V1.0.pdf](晶創25使用者服務介紹-V1.0.pdf) （[GitHub 檢視 ↗](https://github.com/gemini960114/nchc-hpc-bioinfo-course/blob/main/%E6%99%B6%E5%89%B525%E4%BD%BF%E7%94%A8%E8%80%85%E6%9C%8D%E5%8B%99%E4%BB%8B%E7%B4%B9-V1.0.pdf)） | PDF 手冊 | 晶創25 (TAIDE/HPC) 平台服務介紹、帳號與連線、計算資源與規格說明 | 跨平台對照參考手冊 |

---

> 💡 **課後建議實作步驟**：
> 1. 本機啟動 `.\ssh-proxy-windows-x64.exe t3-c4` 並登入。
> 2. 在 Antigravity IDE 透過 `t3-c4-proxy` 連上遠端主機。
> 3. 開啟 `01_fastqc_multiqc_demo.ipynb`，切換到已註冊之生醫 Kernel 執行資料質檢。
> 4. 利用次側邊欄的 AI Agent 協助你撰寫專屬的 SLURM 腳本，送出屬於你的第一個批次排程工作！
